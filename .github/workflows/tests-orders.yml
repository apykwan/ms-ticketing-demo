name: tests-orders

on: 
    push:
        paths:
            - 'orders/**'

    pull_request:
        paths:
            - 'orders/**'


jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
            - run: cd orders && npm install && npm run test:ci